<h1>My Cookbook</h1>
<h2>The best amateur Chef and Home Cook Community</h2>

<div class="grid">
    {{#each recipes}}
    <div><a href="/{{this._id}}">
        <img src= {{ this.image}}>
        <h4>{{ this.title }}</h4>
        <p>by {{ this.creator.username }}</p>
    </a></div>
    {{/each}}
</div>